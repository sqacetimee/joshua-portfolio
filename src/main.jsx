import React from 'react'
import ReactDOM from 'react-dom/client'
import { BrowserRouter } from 'react-router-dom'
import App from './App'
import './index.css'

// The entry screen takes over as soon as the page loads; marking it here keeps titles hidden from the
// first paint, before any component has mounted
document.documentElement.dataset.intro = 'start'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </React.StrictMode>
)
